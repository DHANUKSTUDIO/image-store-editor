# image-store-editor
A modern image store, editor, and management website with upload, edit, and download features
cd the-dhanuk-app/mobile
npm install
npx eas-cli build:configure
npx eas-cli build --platform android --profile preview